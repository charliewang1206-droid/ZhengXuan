# 正选开发计划

这个计划用于把正选从当前工程骨架推进到可以提交 App Store 的 1.0 版本。每个版本都必须满足一个原则：先能稳定运行，再继续加功能。

## 开发原则

- 本地优先：第一版不接服务器、不接第三方云、不做登录。
- 每个阶段都保持 Codemagic 可构建。
- 先打通核心闭环，再扩展细节。
- 数据模型变更要谨慎，重要变更必须更新 README。
- 删除、导入、清空类操作必须有明确确认。
- 每个里程碑完成后打一版 tag，方便回退。

## 版本路线

### v0.1 工程骨架与 CI

目标：让项目能被 Xcode 和 Codemagic 稳定识别、编译。

已包含：
- SwiftUI App 工程。
- iOS 17 最低版本。
- SwiftData model container。
- 五个底部 Tab：首页、分类、帮我选、探索、我的。
- 核心模型：分类、评价字段、标签、条目、体验记录、动态字段值。
- `codemagic.yaml` simulator build。

验收标准：
- Codemagic `ios-simulator-build` 通过。
- Xcode 可打开工程。
- App 可启动到首页。

### v0.2 分类、标签、条目

目标：用户可以建立自己的基础资料库。

范围：
- 分类列表、新建、编辑、删除、排序基础能力。
- 子分类管理。
- 分类评价模板编辑。
- 标签管理：新建、编辑、删除。
- 条目列表、新建、编辑、归档、删除。
- 条目支持主分类、子分类、标签、收藏、想尝试。
- 基础搜索：条目名、分类名、标签名。

验收标准：
- 可以新建“食物 > 方便面”。
- 可以给“方便面”添加“辣度、性价比、饱腹感”等评价字段。
- 可以新建“火鸡面奶油味”并添加标签。
- 空名称不能保存。
- 删除操作有二次确认。
- Codemagic 构建通过。

### v0.3 体验记录核心闭环

目标：完成产品最重要的“刚体验完，记录一下”流程。

范围：
- 快速体验记录页面。
- 完整体验记录页面。
- 编辑体验记录。
- 删除体验记录。
- 动态评价字段填写。
- 条目详情页展示历史记录。
- 条目统计：平均满意度、体验次数、首次体验、最近体验、最近即时印象。
- 首页最近记录和最近条目改为真实数据。

验收标准：
- 30 秒内可以完成一次体验记录。
- 必填项只有总体感觉、再次选择意愿、一句话印象。
- 条目详情能看到所有历史体验记录。
- App 重启后数据仍保留。
- Codemagic 构建通过。

### v0.4 帮我选与探索

目标：让 App 开始帮助用户降低纠结，而不是只做记录。

范围：
- 帮我选筛选条件：分类、已体验、想尝试、当前偏好、风险接受度、标签偏好。
- 推荐服务独立实现。
- 推荐结果分组：最稳妥、最值得再选、很久没选但以前喜欢、可以尝试的新选择、随机惊喜。
- 每个推荐结果显示原因。
- 探索页：想尝试清单、未体验收藏、很久没重访、高评分旧选择、随机探索。

验收标准：
- 可以筛选“方便面 + 辣 + 稳妥”。
- 推荐结果可解释，不是黑盒。
- 推荐逻辑有 deterministic 单元测试。
- Codemagic 构建通过。

### v0.5 导入导出与备份

目标：让用户的数据可以迁移，不被 App 锁死。

范围：
- 全量 JSON 导出。
- JSON 导入：覆盖导入、合并导入。
- 导入前风险提示。
- 导入后结果摘要。
- 体验记录 CSV 导出。
- 系统分享面板。
- schema version 字段。

验收标准：
- 可以导出完整 JSON。
- 可以导出体验记录 CSV。
- 可以从 JSON 恢复主要数据。
- 异常 JSON 不会让 App 崩溃。
- Codemagic 构建通过。

### v0.6 示例数据、统计、打磨

目标：从“功能可用”进入“日常可用”。

范围：
- 首次启动可选择加载示例数据。
- 示例数据覆盖高分、低分、多次体验、想尝试、很久没再选、不同标签。
- 数据统计页。
- 首页统计补全。
- 空状态、错误提示、删除确认统一打磨。
- 深色模式检查。
- Dynamic Type 基础适配。
- 常用页面文案调整。

验收标准：
- 新用户不加载示例数据也能自然开始。
- 用户主动选择后可以加载示例数据。
- 深色模式无明显阅读问题。
- 主要页面没有空白死角。
- Codemagic 构建通过。

### v0.7 内测版 TestFlight

目标：准备真实 iPhone 测试。

范围：
- App icon。
- Launch screen 体验检查。
- Bundle ID 固定。
- Apple Developer Team 配置。
- Release build 配置。
- App Store Connect App 记录。
- TestFlight 内部测试上传。

验收标准：
- 能在真实 iPhone 上安装 TestFlight 版本。
- 离线完整可用。
- 主要流程完成 3 轮真实测试。
- 没有阻断性崩溃。

### v0.8 App Store 提交准备

目标：补齐上架材料与审核要求。

范围：
- App 名称、短描述、长描述。
- 关键词。
- 隐私政策页面或说明。
- App Privacy 填写：无追踪、无第三方数据收集、数据本地存储。
- 截图：至少 iPhone 6.7 英寸和 6.1 英寸。
- 审核备注。
- 版本号和构建号规范。

验收标准：
- App Store Connect 所有必填项完成。
- TestFlight 版本稳定。
- 隐私声明与实际行为一致。
- 无登录、无网络依赖、无广告、无支付。

### v1.0 App Store 首发

目标：提交审核并发布第一个正式版本。

范围：
- 最终 Release archive。
- App Store Review 提交。
- 审核反馈处理。
- 首发版本 release notes。
- GitHub tag：`v1.0.0`。

验收标准：
- App Store 审核通过。
- 用户可从 App Store 下载。
- README 更新到 1.0 状态。

## 近期执行顺序

1. 等 Codemagic 当前构建完成。
2. 如果构建失败，先修到 v0.1 通过。
3. 如果构建成功，进入 v0.2。
4. v0.2 完成后提交 GitHub 并触发 Codemagic。
5. 每个版本都按“实现、静态检查、Codemagic 构建、README 更新”的顺序收尾。

## 上架前必须准备

- Apple Developer Program 账号。
- App Store Connect 访问权限。
- 固定 Bundle ID。
- App icon。
- 隐私说明。
- 至少一台真实 iPhone 测试。
- TestFlight 测试记录。
- App Store 截图与文案。

## 暂不做

- 登录。
- 微信登录。
- 云同步。
- 服务器。
- Firebase。
- Supabase。
- 地图 SDK。
- 支付。
- 广告。
- 社交功能。
- 公开评价。
- 复杂 AI 推荐。


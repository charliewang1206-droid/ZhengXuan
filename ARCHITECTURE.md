# 正选工程架构说明

这个文档记录我们做正选时采用的工程约束。目标不是把第一版写得很重，而是让每一步都有稳定边界，避免后续返工。

## 总体结构

- `App`：应用入口、Tab 导航、全局文案和配置。
- `Design`：颜色、视觉常量和未来的通用组件约定。
- `Models`：SwiftData 模型、枚举和模型衍生计算。
- `Features`：按功能拆页面。页面可以持有轻量 UI 状态，但不承担复杂业务算法。
- `Services`：推荐、导入导出、删除策略、示例数据等可测试逻辑。复杂逻辑先放这里，不塞进 View。

## SwiftData 规则

- 所有核心对象使用 `UUID` 作为稳定 id。
- 删除策略必须保守：能归档时优先归档；硬删除必须有二次确认。
- 分类支持父子关系，但不做无限层级 UI。第一版允许多层数据结构，界面优先保持简单。
- 防止循环分类：编辑父分类时不能选择自己或自己的后代。
- 动态评价字段用 `ExperienceFieldValue` 保存结构化值，不把所有字段塞进一个 JSON 字符串。
- 图片第一版只记录本地路径，导入导出阶段再统一处理图片备份方案。

## UI 规则

- 首屏优先让用户快速记录和快速选择。
- 表单只展示必要字段，高级字段放到后续阶段的“更多细节”。
- 使用系统 `List`、`Form`、`NavigationStack`、`Sheet`、`ConfirmationDialog`。
- 所有删除动作都必须确认。
- 空状态必须可理解，并给出下一步动作。

## 功能推进规则

每个版本都按这个顺序推进：

1. 明确版本目标和验收标准。
2. 实现最小可用闭环。
3. 补输入校验、删除确认、空状态。
4. 更新 `PROGRESS_LOG.md`。
5. 提交 GitHub。
6. Codemagic 通过后进入下一阶段。

## v0.2 边界

v0.2 只做“基础资料库”：

- 分类 CRUD。
- 子分类。
- 分类评价字段。
- 标签 CRUD。
- 条目 CRUD。
- 基础搜索。

v0.2 不做：

- 体验记录填写。
- 推荐算法。
- 导入导出。
- 示例数据。

这些留给 v0.3 到 v0.6，避免一次混入太多变量。

